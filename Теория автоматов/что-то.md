регулярная формула (a* + b*)c
c, ac, bc, abc. 

(aa+bb)*

конечный автомат может быть детерминированный
(s,x) => S'
(s,x) => S''

любой недетерминированный автомат можно преобразовать в детерминированный.